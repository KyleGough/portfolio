name: Pre-Merge

on:
  push:
    branches:
      - master
      - develop
  pull_request:
    branches:
      - master
      - develop
  workflow_dispatch:

env:
  CI: true

jobs:
  lint-ci:
    name: Lint CI
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: Clean Install
        run: npm ci

      - name: Build Tailwind
        run: npm run build:tailwind

      - name: ESLint
        run: npm run lint

  test-ci:
    name: Test CI
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: Clean Install
        run: npm ci

      - name: Tests
        run: npm test

  build-ci:
    name: Build CI
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: Clean Install
        run: npm ci

      - name: Build
        run: npm run build
